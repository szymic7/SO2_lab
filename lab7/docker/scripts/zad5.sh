# 5. docker-compose

# docker compose up
sudo docker compose up --build -d

# docker compose down (po utworzeniu uzytkownika)
sudo docker compose down

# ponowne docker compose up (juz bez build - obraz 'backend' jest juz zbudowany)
sudo docker compose up -d

